name: Build APK

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Show project structure
        run: ls -R

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Find Gradle wrapper
        run: find . -name gradlew

      - name: Grant execute permission
        run: chmod +x AhMyth-Android-RAT-master/AhMyth-Client/gradlew || true

      - name: Build APK (Debug)
        run: |
          cd AhMyth-Android-RAT-master/AhMyth-Client
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: AhMyth-Android-RAT-master/AhMyth-Client/app/build/outputs/apk/debug/*.apk

